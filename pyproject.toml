[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "saltmill-spark"
version = "0.2.4"
description = "Efficient large-CSV processing for Apache Spark / Databricks with auto-salting, skew detection, and partition tuning"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.10"
keywords = ["pyspark", "databricks", "csv", "big-data", "partitioning", "salting", "skew"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Information Analysis",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

# pyspark is a peer dependency — users bring their own cluster version
dependencies = []

[project.optional-dependencies]
databricks = ["databricks-sdk>=0.12"]
dev = [
    "pytest>=8",
    "pytest-asyncio",
    "pyspark>=3.4",
    "delta-spark>=2.4",
    "ruff",
    "mypy",
]

[project.urls]
Homepage = "https://github.com/yuvaraj-munirathinam/saltmill"
Repository = "https://github.com/yuvaraj-munirathinam/saltmill"
Issues = "https://github.com/yuvaraj-munirathinam/saltmill/issues"

[tool.setuptools.packages.find]
where = ["."]
include = ["saltmill*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]

[tool.ruff]
line-length = 100
target-version = "py310"

# Pin the rule set explicitly so `ruff check` is deterministic across ruff
# versions (the default rule set drifts between releases, which caused CI to
# flag rules that a locally-installed ruff did not).
[tool.ruff.lint]
# BLE (blind-except) is intentionally omitted: saltmill deliberately catches
# broad exceptions to degrade gracefully across Databricks cluster types
# (Spark Connect / serverless, where JVM access raises).
select = ["E", "F", "I", "UP", "W", "B", "G", "RUF"]

[tool.mypy]
strict = true
ignore_missing_imports = true
